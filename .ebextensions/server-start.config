option_settings:
  # Health-check the API (nginx -> node). Startup is governed by the root
  # Procfile (build + web targets), not any NodeCommand.
  aws:elasticbeanstalk:environment:process:default:
    HealthCheckPath: /health
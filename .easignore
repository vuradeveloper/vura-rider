# Large build artifacts that don't belong in the EAS upload
*.apk
*.aab
node_modules/
.expo/
dist/
web-build/
# Native projects are generated by EAS; never upload stale prebuild folders
/android
/ios
